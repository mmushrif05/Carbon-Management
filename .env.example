# ===== CarbonTrack Pro — Environment Variables =====
# Copy this file to .env and fill in your values.
# NEVER commit .env to version control.

# --- Project Configuration ---
# These control branding and database paths.
PROJECT_ID=default
PROJECT_NAME=My Construction Project
APP_BRAND=CarbonTrack Pro
APP_TAGLINE=Construction Embodied Carbon Platform

# --- Firebase ---
# Create a Firebase project at https://console.firebase.google.com
# Enable Realtime Database and Authentication (Email/Password).
FIREBASE_API_KEY=your-firebase-api-key
FIREBASE_DATABASE_URL=https://your-project.firebaseio.com
FIREBASE_SERVICE_ACCOUNT=base64-encoded-service-account-json

# --- AI (Anthropic Claude) ---
# Get an API key at https://console.anthropic.com
# Used for BOQ parsing and carbon advisory features.
ANTHROPIC_API_KEY=your-anthropic-api-key

# --- Email (SMTP) ---
# Required for invitation emails and password resets.
# If not set, email features are disabled gracefully.
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your-email@example.com
SMTP_PASS=your-email-password
SMTP_FROM=noreply@example.com

# --- Security ---
# AES-256 encryption key (64 hex characters). Generate with:
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
DATA_ENCRYPTION_KEY=your-64-char-hex-key

# Privacy level: standard | enhanced | maximum
DATA_PRIVACY_LEVEL=standard

# CORS allowed origins (comma-separated). Leave empty for dev mode.
ALLOWED_ORIGINS=https://your-app.netlify.app

# --- Setup ---
# One-time key for creating the first admin user via /api/bootstrap.
# Set any strong random string. Remove after initial setup.
BOOTSTRAP_KEY=your-random-setup-key

# --- IoT Monitoring (OpenWeatherMap) ---
# Free API key from https://openweathermap.org/api (Air Pollution API)
# Used by the Live IoT Monitor module for real-time air quality data.
# Users can also enter their key directly in the IoT Monitor UI.
OPENWEATHERMAP_API_KEY=your-openweathermap-api-key

# --- Application URL ---
# Used in email templates for links. Netlify sets URL automatically.
APP_URL=https://your-app.netlify.app
